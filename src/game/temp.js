// import game from './main.js'
// import Board from './gameboardFactory.js'
//import {renderBoard} from '../components/boardRender.js'


// let ship1 = document.getElementById('ship1')

// let newGame = document.getElementById('newGame')

// ship1.addEventListener('click', (e) => {    
//     let payload = {
//     x: e.target.dataset.positionX,
//     y: e.target.dataset.positionY
// }})


// newGame.addEventListener('click', () => {
//     game.dispatch('setBoards', Board())
// })

// game.dispatch('newGameButton')

// game.dispatch('selectAxis', 'y')

// game.dispatch('selectShip', 'Destroyer')

// game.dispatch('clickBoard', {x: 0, y: 0})

// game.dispatch('selectShip', 'Carrier')

// game.dispatch('clickBoard', {x: 1, y: 0})

// game.dispatch('selectShip', 'Battleship')

// game.dispatch('clickBoard', {x: 2, y: 0})

// game.dispatch('selectShip', 'Cruiser')

// game.dispatch('clickBoard', {x: 3, y: 0})

// game.dispatch('selectShip', 'Submarine')

// game.dispatch('clickBoard', {x: 4, y: 0})

// game.dispatch('clickBoard', {x: 4, y: 0})

// game.dispatch('clickBoard', {x: 0, y: 0})

// game.dispatch('clickBoard', {x: 1, y: 0})

// game.dispatch('clickBoard', {x: 2, y: 0})

// game.dispatch('clickBoard', {x: 3, y: 0})

// game.dispatch('clickBoard', {x: 4, y: 0})

// game.dispatch('clickBoard', {x: 0, y: 1})

// game.dispatch('clickBoard', {x: 1, y: 1})

// game.dispatch('clickBoard', {x: 2, y: 1})

// game.dispatch('clickBoard', {x: 3, y: 1})

// game.dispatch('clickBoard', {x: 4, y: 1})

// game.dispatch('clickBoard', {x: 0, y: 2})

// game.dispatch('clickBoard', {x: 1, y: 2})

// game.dispatch('clickBoard', {x: 2, y: 2})

// game.dispatch('clickBoard', {x: 3, y: 2})

// game.dispatch('clickBoard', {x: 4, y: 2})

// game.dispatch('clickBoard', {x: 0, y: 3})

// game.dispatch('clickBoard', {x: 1, y: 3})

// game.dispatch('clickBoard', {x: 2, y: 3})

// game.dispatch('clickBoard', {x: 3, y: 3})

// game.dispatch('clickBoard', {x: 4, y: 3})

// game.dispatch('clickBoard', {x: 0, y: 4})

// game.dispatch('clickBoard', {x: 1, y: 4})

// game.dispatch('clickBoard', {x: 2, y: 4})

// game.dispatch('clickBoard', {x: 3, y: 4})

// game.dispatch('clickBoard', {x: 4, y: 4})

// game.dispatch('clickBoard', {x: 5, y: 5})

// game.dispatch('clickBoard', {x: 6, y: 6})

// game.dispatch('newGameButton')

